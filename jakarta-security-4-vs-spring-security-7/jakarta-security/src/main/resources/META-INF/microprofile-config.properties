mp.jwt.verify.publickey.location=https://auth.enterprise.com/realms/myrealm/protocol/openid-connect/certs
mp.jwt.verify.issuer=https://auth.enterprise.com/realms/myrealm

# Placeholders for the OpenID definition in SecuritySetup: supply real values via environment variables
oidc.provider.url=https://auth.enterprise.com/realms/myrealm
oidc.client.id=change-me
# oidc.client.secret: set via env var OIDC_CLIENT_SECRET, never commit it
