mp.jwt.verify.publickey.location=http://localhost:8080/realms/webcodein-realm/protocol/openid-connect/certs
mp.jwt.verify.issuer=http://localhost:8080/realms/webcodein-realm

# Placeholders for the OpenID definition in SecuritySetup: supply real values via environment variables
oidc.provider.url=http://localhost:8080/realms/webcodein-realm
oidc.client.id=spring-boot-client
# oidc.client.secret: set via env var OIDC_CLIENT_SECRET, never commit it
